<% content_for(:title) do %>
  <%= @rooftop.name %>
<% end %>

<div class="show-banner">
</div>

<div class="container">
  <div class="rooftop-info">
    <h2 class="name"><%= @rooftop.name %></h2>
    <p class="description"><%= @rooftop.description %></p>
    <p class="address"><%= @rooftop.address %></p>
    <p class="price"><%= @rooftop.price %>€</p>
  </div>
  <%= link_to "Delete", rooftop_path(@rooftop),
        method: :delete,
        class: "btn btn-danger",
        data: { confirm: "Are you sure?" } %>
</div>

